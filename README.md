# Panther Performance Matrix

A centralized performance dashboard for Park Tudor AMS, integrating Output Sports metrics, InBody scans, and live athletics schedules.

## 🚀 Deployment Guide (Netlify)

This project is configured for **Netlify** with Serverless Functions for the calendar proxy.

---

## 1. Prerequisites

You will need the following API keys/credentials:

- **Google Gemini API Key** (for AI features)
- **Output Sports Credentials** (Email & Password for your organization)

---

## 2. Push to GitHub

Run the following commands in your project folder:

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin <your-repo-url>
git push -u origin main
